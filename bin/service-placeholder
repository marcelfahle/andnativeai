#!/bin/sh
set -eu

service_name="${1:-${SERVICE_ROLE:-service}}"

echo "${service_name} placeholder started. This service will be replaced by the PoC runtime implementation."

while true; do
  sleep 3600
done
